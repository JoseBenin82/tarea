package INVENTARIOPRODUCTO;
import javax.swing.*;
public class Ventana extends JFrame {
	Ventana(int x, int y, int ancho, int alto, String titulo){
		setTitle(titulo);
		setBounds(x,y,ancho,alto);
		
	}

}
