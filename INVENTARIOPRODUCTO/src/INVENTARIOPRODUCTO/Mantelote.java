package INVENTARIOPRODUCTO;
import javax.swing.*;
import java.awt.Color;
public class Mantelote extends JPanel {
 public Mantelote(int x, int y, int ancho, int alto) {
	 
	 setLayout(null);
	 setBounds(x,y,ancho,alto);
	 setBackground(new Color(0,128,0));
 }
}
